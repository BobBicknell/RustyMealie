{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the RustyMeals main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
